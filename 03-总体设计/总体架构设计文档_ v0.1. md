# 拼豆系统总体架构设计文档

> **项目**: 拼豆 - AI 照片转拼豆工具
> **版本**: V1.0
> **编制**:
> **日期**: 2026-06-07

---

## 1. 系统概述

### 1.1 项目背景

**拼豆**是一款面向C端手工爱好者的AI拼豆图纸生成工具，核心价值主张：**用AI 1分钟把照片转拼豆图纸，让新手也能完成自己的第一个作品**。

系统包含三大组成部分：
1. **移动端APP**（Flutter）- 面向用户的功能入口
2. **业务后端服务** - 核心业务逻辑处理
3. **后台管理系统**（Vue3+Spring Boot）- 运营管理平台

### 1.2 系统边界

```┌─────────────────────────────────────────────────────────────────────┐
│                          用户层                                       │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐  │
│  │   iOS用户       │    │  Android用户    │    │   运营人员      │  │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      移动端层 (Flutter)                              │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  M1 注册登录 │ M2 照片生成 │ M3 论坛 │ M4 模板 │ M5 色号推荐 │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         业务后端服务层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │   API网关     │  │  认证服务    │  │  用户服务    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  拼豆服务    │  │  模板服务    │  │  论坛服务    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  消息服务    │  │  AI推理服务  │  │  推荐服务    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         数据存储层                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │    MySQL     │  │    Redis     │  │     OSS      │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐                               │
│  │ Elasticsearch│  │   Kafka      │                               │
│  └──────────────┘  └──────────────┘                               │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     后台管理系统层 (Vue3+Spring Boot)                │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 用户管理 │ 内容管理 │ 模板管理 │ 运营管理 │ 数据统计 │ 系统配置│   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. 技术架构

### 2.1 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **移动端** | Flutter 3.x | 跨平台(iOS/Android) |
| **后台前端** | Vue 3.x + Element Plus | 管理后台界面 |
| **后端框架** | Spring Boot 3.x | 业务API服务 |
| **AI推理** | Python + PyTorch | GPU推理 |
| **数据库** | MySQL 8.0 | 核心业务数据 |
| **缓存** | Redis 7.x | 会话、热点数据 |
| **文件存储** | 阿里云OSS | 图片、视频存储 |
| **搜索** | Elasticsearch 8.x | 全文搜索 |
| **消息队列** | Kafka | 异步任务、消息通知 |
| **网关** | Kong/Spring Cloud Gateway | 统一入口 |
| **容器** | Kubernetes | 容器编排 |

### 2.2 架构分层

```┌─────────────────────────────────────────────────────────────────────┐
│                         接入层                                        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ CDN     │ │ 负载均衡 │ │ API网关  │ │ WSS服务  │ │ 推送服务 │       │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         应用层                                        │
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │                     移动端应用服务                               ││
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              ││
│  │  │ 认证API │ │ 拼豆API │ │ 论坛API │ │ 模板API │              ││
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘              ││
│  └─────────────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │                     后台管理系统服务                             ││
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              ││
│  │  │ 用户管理 │ │ 内容审核 │ │ 运营管理 │ │ 数据统计 │              ││
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘              ││
│  └─────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         领域层                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │  用户域   │ │  创作域   │ │  社区域   │ │  消息域   │              │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                            │
│  │  支付域   │ │  审核域   │ │  推荐域   │                            │
│  └──────────┘ └──────────┘ └──────────┘                            │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         基础设施层                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │ 数据库   │ │  缓存    │ │ 文件存储 │ │ 搜索引擎 │              │
│  │ MySQL    │ │ Redis    │ │ OSS      │ │ ES       │              │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │消息队列  │ │ 日志    │ │ 监控    │ │ 安全    │              │
│  │ Kafka    │ │ ELK     │ │Prometheus│ │ WAF     │              │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 系统模块划分

### 3.1 移动端模块（M1-M5）

| 模块 | 功能 | 优先级 |
|------|------|--------|
| M1 注册登录 | 手机号/微信/Apple ID/游客登录 | P0 |
| M2 照片生成拼豆 | 选图→编辑→参数→AI生成→预览 | P0 |
| M5 色号推荐 | 色号表/导出/难度调整/耗材推荐 | P0 |
| M3 论坛 | 信息流/帖子/互动/消息 | P1 |
| M4 经典模板 | 模板库/详情/投稿 | P1 |

### 3.2 后台管理系统模块

| 模块 | 功能 | 说明 |
|------|------|------|
| 登录与权限 | 登录/登出/角色权限/操作日志 | 基础模块 |
| 用户管理 | 用户列表/详情/禁用/会员管理 | 用户运营 |
| 内容管理 | 帖子审核/评论审核/敏感词/举报 | 内容运营 |
| 模板管理 | 模板审核/分类/标签 | 内容运营 |
| 运营管理 | Banner/话题/专题/推送 | 运营配置 |
| 数据统计 | 核心指标/用户/创作/社区分析 | 数据分析 |
| 系统配置 | 通用配置/AI配置/色板配置 | 系统管理 |

---

## 4. 数据架构

### 4.1 数据库划分

```┌─────────────────────────────────────────────────────────────────────┐
│                        数据库划分                                    │
├─────────────────────────────────────────────────────────────────────┤
│  库名               │ 说明                │ 表数量  │ 核心表         │
│  ──────────────────┼────────────────────┼─────────┼────────────    │
│  pindou_user       │ 用户中心            │ 10      │ user,session   │
│  pindou_creation   │ 创作中心(M2/M5)     │ 20      │ diagram,color  │
│  pindou_community  │ 社区(M3/M4)         │ 25      │ post,comment   │
│  pindou_pay        │ 支付中心            │ 15      │ order,member   │
│  pindou_msg        │ 消息中心            │ 8       │ notification   │
│  pindou_admin      │ 后台管理            │ 20+     │ admin_user     │
│  pindou_stats      │ 统计数据            │ 10      │ daily_stats    │
└─────────────────────────────────────────────────────────────────────┘
```### 4.2 核心实体关系

```用户 (User)
    │    ├─→ 拼豆图纸 (Diagram) ──→ 色号 (ColorInfo)
    │    ├─→ 帖子 (Post) ──→ 评论 (Comment)
    │    │           ├─→ 点赞 (Like)
    │    │           └─→ 收藏 (Favorite)
    │    ├─→ 关注 (Follow)
    │    ├─→ 模板收藏 (TemplateFavorite)
    │    └─→ 通知 (Notification)
    │
    ├─→ 会员 (Member)
    │
    └─→ 订单 (Order)
```---

## 5. API架构

### 5.1 移动端API

```API路由规范
───────────────
/api/v1/
    ├── /auth/          # 认证相关 (M1)
    ├── /user/          # 用户相关
    ├── /image/         # 图片服务 (M2)
    ├── /diagram/       # 拼豆图服务 (M2)
    ├── /template/      # 模板服务 (M4)
    ├── /post/          # 帖子服务 (M3)
    ├── /feed/          # 信息流服务 (M3)
    ├── /message/       # 消息服务
    ├── /follow/        # 关注服务 (M3)
    └── /topic/         # 话题服务 (M3)
```

### 5.2 后台管理API

```API路由规范
───────────────
/api/admin/v1/
    ├── /auth/          # 认证
    ├── /user/          # 用户管理
    ├── /member/        # 会员管理
    ├── /post/          # 帖子审核
    ├── /comment/       # 评论审核
    ├── /sensitive/     # 敏感词管理
    ├── /report/        # 举报管理
    ├── /template/      # 模板管理
    ├── /banner/        # Banner管理
    ├── /topic/         # 话题管理
    ├── /push/          # 推送管理
    ├── /stats/         # 数据统计
    └── /config/        # 系统配置
```---

## 6. 安全架构

### 6.1 认证与授权

**移动端认证流程**：
1. 客户端存储access_token(内存)和refresh_token(安全存储)
2. 每次API请求携带Authorization: Bearer {access_token}
3. API网关验证token有效性
4. token过期前10分钟自动用refresh_token刷新
5. refresh_token过期引导重新登录

**后台管理认证**：
- 账号密码登录+JWT Token
- 角色权限控制(RBAC)
- 操作日志记录

### 6.2 安全措施

| 安全项 | 移动端 | 后台管理 |
|--------|--------|----------|
| 传输加密 | HTTPS+TLS 1.3 | HTTPS+TLS 1.3 |
| 数据加密 | AES-256敏感字段 | AES-256密码 |
| API鉴权 | JWT+签名 | JWT |
| 防刷 | 限流+设备指纹 | 验证码+限流 |
| 内容安全 | AI审核+人工 | AI审核+人工 |
| 隐私合规 | 最小化收集 | 数据脱敏 |

---

## 7. 部署架构

### 7.1 部署拓扑

```┌─────────────────────────────────────────────────────────────────────┐
│                        部署架构图                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   ┌─────────────┐                                                   │
│   │   CDN       │  静态资源加速                                      │
│   └──────┬──────┘                                                   │
│          │                                                          │
│   ┌──────▼──────┐                                                   │
│   │   负载均衡   │  L4/L7 负载均衡                                    │
│   └──────┬──────┘                                                   │
│          │                                                          │
│   ┌──────▼──────────────────────────────────────────┐              │
│   │                 API 网关 (Kong)                  │              │
│   │    限流 | 鉴权 | 路由 | 日志                      │              │
│   └──────┬──────────────────────────────────────────┘              │
│          │                                                          │
│   ┌──────▼──────────────────────────────────────────┐              │
│   │              业务服务集群 (K8s)                   │              │
│   │  ┌────────┐  ┌────────┐  ┌────────┐             │              │
│   │  │用户服务 │  │创作服务 │  │社区服务 │             │              │
│   │  └────────┘  └────────┘  └────────┘             │              │
│   │  ┌────────┐  ┌────────┐  ┌────────┐             │              │
│   │  │AI推理服务│ │模板服务 │  │后台服务 │             │              │
│   │  └────────┘  └────────┘  └────────┘             │              │
│   └──────┬──────────────────────────────────────────┘              │
│          │                                                          │
│   ┌──────▼──────┐    ┌──────┐    ┌──────┐    ┌──────┐            │
│   │   MySQL     │    │Redis │    │ OSS  │    │ ES   │            │
│   │  (主从)     │    │集群  │    │对象存储│   │搜索  │            │
│   └─────────────┘    └──────┘    └──────┘    └──────┘            │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.2 容灾设计

| 容灾项 | 方案 |
|--------|------|
| 多活 | 同城双活/异地多活 |
| 数据库 | 主从自动切换 |
| 缓存 | Redis Cluster |
| 存储 | OSS跨区域复制 |
| 服务 | K8s自动恢复 |

---

## 8. 模块依赖关系

### 8.1 模块间依赖

```模块依赖关系
─────────────
           M1  M2  M3  M4  M5  会员  推送  支付  后台
┌─────────┼────┼────┼────┼────┼─────┼────┬────┬─────┐
M1注册登录│ -  │ ○  │ ○  │ ○  │ ○   │    │    │     │
M2照片生成│ ○  │ -  │ ○  │    │ ●   │ ○  │    │     │
M3论坛    │ ○  │ ○  │ -  │ ○  │ ○   │    │    │     │
M4模板    │ ○  │ ●  │ ○  │ -  │ ○   │    │    │     │
M5色号推荐│ ○  │ ●  │    │    │ -   │    │    │     │
会员系统  │    │ ○  │    │    │     │ -  │    │     │
推送服务  │ ○  │ ○  │ ○  │ ○  │ ○   │    │ -  │     │
支付系统  │    │ ○  │    │    │     │ ○  │    │ -   │
后台管理  │    │ ○  │ ○  │ ○  │ ○   │ ○  │ ○  │ -   │
└─────────┴────┴────┴────┴────┴─────┴────┴────┴─────┘
● = 强依赖  ○ = 弱依赖
```### 8.2 核心业务流程

**主业务流程**：
```
注册/登录 (M1) → 选图 (M2) → 编辑 (M2) → 参数配置 (M2)
    → AI生成 (M2) → 预览 (M2) → 色号表 (M5) → 导出/分享
```

**社区流程**：
```
发布帖子 (M3) → 审核 → 展示 → 互动 → 消息通知
```

**模板流程**：
```
模板库 (M4) → 按此生成 → M2创作页 → 结果
```

---

## 9. 性能设计

### 9.1 性能指标

| 指标 | 移动端 | 后台管理 | 后端API |
|------|--------|----------|---------|
| 启动/加载 | <1.5s | <1s | - |
| 响应时间 | - | - | P95<200ms |
| AI生成 | P95<30s | - | - |
| 信息流首屏 | <1s | - | - |
| 列表滑动 | 60fps | - | - |

### 9.2 性能优化策略

- **缓存**：多级缓存(本地+Redis)，热点数据预加载
- **异步**：AI生成异步化，消息队列削峰
- **CDN**：静态资源加速，图片懒加载
- **数据库**：索引优化，读写分离

---

## 10. 监控体系

### 10.1 监控维度

| 维度 | 内容 |
|------|------|
| 基础设施 | CPU/内存/磁盘/网络 |
| 应用服务 | API响应时间/错误率 |
| 业务指标 | 注册数/生成次数/DAU |
| AI监控 | 推理延迟/成功率 |

### 10.2 告警策略

| 级别 | 场景 | 响应时间 |
|------|------|----------|
| P0 | 服务不可用 | 5分钟 |
| P1 | 核心接口延迟>1s | 15分钟 |
| P2 | 错误率>5% | 30分钟 |

---

## 11. 附录

### 11.1 版本历史

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v0.1 | 2026-06-07 | - | 初稿，基于PRD M1-5 |

### 11.2 关联文档

- 《PRD_M1_注册登录_v0.1.md》
- 《PRD_M2_照片转拼豆_v0.1.md》
- 《PRD_M3_论坛_v0.1.md》
- 《PRD_M4_经典拼图模板_v0.1.md》
- 《PRD_M5_色号推荐_v0.1.md》
- 《移动端需求规格说明书》
- 《后台管理系统需求规格说明书》

---

*总体架构设计文档结束*