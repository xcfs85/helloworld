# 拼豆系统总体架构设计文档

> **项目**: 拼豆 - AI 照片转拼豆工具
> **版本**: V0.2
> **编制**:
> **日期**: 2026-06-07

---

## 1. 系统概述

### 1.1 项目背景

**拼豆**是一款面向C端手工爱好者的AI拼豆图纸生成工具，核心价值主张：**用AI 1分钟把照片转拼豆图纸，让新手也能完成自己的第一个作品**。

系统包含三大组成部分：
1. **移动端APP**（Flutter）- 面向用户的功能入口
2. **业务后端服务** - 核心业务逻辑处理（C# ASP.NET）
3. **后台管理系统**（Vue3+ASP.NET）- 运营管理平台

### 1.2 系统边界

```
┌─────────────────────────────────────────────────────────────────────┐
│                          用户层                                       │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐  │
│  │   iOS用户       │    │  Android用户    │    │   运营人员      │  │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      移动端层 (Flutter)                              │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │  M1 注册登录 │ M2 照片生成 │ M3 论坛 │ M4 模板 │ M5 色号推荐 │   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      业务后端服务层 (C# ASP.NET)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │   认证服务    │  │  用户服务    │  │  拼豆服务    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │  模板服务    │  │  论坛服务    │  │  消息服务    │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐                               │
│  │  AI生图服务  │  │  推荐服务    │                               │
│  └──────────────┘  └──────────────┘                               │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         数据存储层                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │
│  │   SQLite     │  │    Redis     │  │  本地文件存储 │              │
│  │ (开发环境)   │  │              │  │              │              │
│  └──────────────┘  └──────────────┘  └──────────────┘              │
│  ┌──────────────┐  ┌──────────────┐                               │
│  │ PostgreSQL   │  │              │                               │
│  │ (生产环境)   │  │              │                               │
│  └──────────────┘  └──────────────┘                               │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     后台管理系统层 (Vue3+ASP.NET)                    │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │ 用户管理 │ 内容管理 │ 模板管理 │ 运营管理 │ 数据统计 │ 系统配置│   │
│  └──────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. 技术架构

### 2.1 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **移动端** | Flutter 3.x | 跨平台(iOS/Android) |
| **后台前端** | Vue 3.x + Element Plus | 管理后台界面 |
| **后端框架** | ASP.NET 8.x | 业务API服务，C#开发 |
| **ORM** | SqlSugar | 数据库ORM框架 |
| **数据库** | SQLite (开发) / PostgreSQL (生产) | 数据存储 |
| **缓存** | Redis 7.x | 会话、热点数据 |
| **文件存储** | 本地存储 | 图片、文件存储 |
| **AI生图** | 阿里云通义万相 / 百度智能云 | 云端AI生图服务 |
| **容器** | Docker | 容器化部署 |

### 2.2 架构分层

```
┌─────────────────────────────────────────────────────────────────────┐
│                         接入层                                        │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                   │
│  │ CDN     │ │ 负载均衡 │ │ WSS服务  │ │ 推送服务 │                   │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘                   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         应用层                                        │
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │                     移动端应用服务                               ││
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              ││
│  │  │ 认证API │ │ 拼豆API │ │ 论坛API │ │ 模板API │              ││
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘              ││
│  └─────────────────────────────────────────────────────────────────┘│
│  ┌─────────────────────────────────────────────────────────────────┐│
│  │                     后台管理系统服务                             ││
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              ││
│  │  │ 用户管理 │ │ 内容审核 │ │ 运营管理 │ │ 数据统计 │              ││
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘              ││
│  └─────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         领域层                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │  用户域   │ │  创作域   │ │  社区域   │ │  消息域   │              │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                            │
│  │  支付域   │ │  审核域   │ │  推荐域   │                            │
│  └──────────┘ └──────────┘ └──────────┘                            │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         基础设施层                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                            │
│  │ 数据库   │ │  缓存    │ │ 文件存储 │                            │
│  │ SQLite   │ │ Redis    │ │ 本地存储  │                            │
│  │PostgreSQL│ │          │ │          │                            │
│  └──────────┘ └──────────┘ └──────────┘                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                            │
│  │ 日志    │ │ 监控    │ │ 外部服务  │                            │
│  │ 文件    │ │ 基础日志 │ │ AI云服务  │                            │
│  └──────────┘ └──────────┘ └──────────┘                            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 3. 系统模块划分

### 3.1 移动端模块（M1-M5）

| 模块 | 功能 | 优先级 |
|------|------|--------|
| M1 注册登录 | 手机号/微信/Apple ID/游客登录 | P0 |
| M2 照片生成拼豆 | 选图→编辑→参数→AI生成→预览 | P0 |
| M5 色号推荐 | 色号表/导出/难度调整/耗材推荐 | P0 |
| M3 论坛 | 信息流/帖子/互动/消息 | P1 |
| M4 经典模板 | 模板库/详情/投稿 | P1 |

### 3.2 后台管理系统模块

| 模块 | 功能 | 说明 |
|------|------|------|
| 登录与权限 | 登录/登出/角色权限/操作日志 | 基础模块 |
| 用户管理 | 用户列表/详情/禁用/会员管理 | 用户运营 |
| 内容管理 | 帖子审核/评论审核/敏感词/举报 | 内容运营 |
| 模板管理 | 模板审核/分类/标签 | 内容运营 |
| 运营管理 | Banner/话题/专题/推送 | 运营配置 |
| 数据统计 | 核心指标/用户/创作/社区分析 | 数据分析 |
| 系统配置 | 通用配置/AI配置/色板配置 | 系统管理 |

---

## 4. 数据架构

### 4.1 数据库设计

使用SqlSugar ORM框架，支持多数据库类型：

| 环境 | 数据库 | 适用场景 |
|------|--------|----------|
| 开发环境 | SQLite | 快速开发、单机运行 |
| 生产环境 | PostgreSQL | 高并发、生产部署 |

**数据库划分**：

```
┌─────────────────────────────────────────────────────────────────────┐
│                        数据库划分                                    │
├─────────────────────────────────────────────────────────────────────┤
│  表分类             │ 说明                │ 核心表                  │
│  ──────────────────┼────────────────────┼────────────             │
│  用户中心           │ 用户、认证、会话    │ User, Session, Token    │
│  创作中心           │ 拼豆图纸、色号      │ Diagram, ColorInfo      │
│  社区               │ 帖子、评论、互动    │ Post, Comment, Like     │
│  模板               │ 模板、分类、标签    │ Template, Category      │
│  消息               │ 通知、消息          │ Notification, Message   │
│  支付               │ 订单、会员          │ Order, Member           │
│  运营               │ Banner、话题、专题  │ Banner, Topic           │
│  后台               │ 管理员、权限、日志  │ AdminUser, Role, Log    │
│  统计               │ 日统计              │ DailyStats              │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.2 核心实体关系

```
用户 (User)
    │    ├─→ 拼豆图纸 (Diagram) ──→ 色号 (ColorInfo)
    │    ├─→ 帖子 (Post) ──→ 评论 (Comment)
    │    │           ├─→ 点赞 (Like)
    │    │           └─→ 收藏 (Favorite)
    │    ├─→ 关注 (Follow)
    │    ├─→ 模板收藏 (TemplateFavorite)
    │    └─→ 通知 (Notification)
    │
    ├─→ 会员 (Member)
    │
    └─→ 订单 (Order)
```

---

## 5. API架构

### 5.1 移动端API

```
API路由规范
───────────────
/api/v1/
    ├── /auth/          # 认证相关 (M1)
    ├── /user/          # 用户相关
    ├── /image/         # 图片服务 (M2)
    ├── /diagram/       # 拼豆图服务 (M2)
    ├── /template/      # 模板服务 (M4)
    ├── /post/          # 帖子服务 (M3)
    ├── /feed/          # 信息流服务 (M3)
    ├── /message/       # 消息服务
    ├── /follow/        # 关注服务 (M3)
    └── /topic/         # 话题服务 (M3)
```

### 5.2 后台管理API

```
API路由规范
───────────────
/api/admin/v1/
    ├── /auth/          # 认证
    ├── /user/          # 用户管理
    ├── /member/        # 会员管理
    ├── /post/          # 帖子审核
    ├── /comment/       # 评论审核
    ├── /sensitive/     # 敏感词管理
    ├── /report/        # 举报管理
    ├── /template/      # 模板管理
    ├── /banner/        # Banner管理
    ├── /topic/         # 话题管理
    ├── /push/          # 推送管理
    ├── /stats/         # 数据统计
    └── /config/        # 系统配置
```

---

## 6. AI生图服务

### 6.1 服务选型

推荐使用**阿里云通义万相**或**百度智能云**作为AI生图服务：

| 服务商 | 产品 | 特点 |
|--------|------|------|
| 阿里云 | 通义万相 | 文字生成图像、图像生成图像，API稳定 |
| 百度智能云 | 文心一格 | 国产大模型，本土化服务好 |

### 6.2 集成架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                       AI生图服务架构                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────┐                                                  │
│  │  拼豆服务    │                                                  │
│  │  (C# ASP.NET)│                                                  │
│  └──────┬───────┘                                                  │
│         │ 调用AI生图API                                            │
│         ▼                                                          │
│  ┌──────────────────────────────────────────────────────────┐      │
│  │                    外部AI生图服务                          │      │
│  │  ┌─────────────────┐   ┌─────────────────┐               │      │
│  │  │  阿里云通义万相  │   │  百度智能云     │               │      │
│  │  │  (首选)          │   │  (备选)         │               │      │
│  │  └─────────────────┘   └─────────────────┘               │      │
│  └──────────────────────────────────────────────────────────┘      │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 6.3 服务特点

- **即开即用**：无需自建GPU服务器，调用API即可
- **按量计费**：按调用次数收费，成本可控
- **稳定可靠**：云服务厂商提供高可用保障
- **快速集成**：提供RESTful API，易于集成

---

## 7. 安全架构

### 7.1 认证与授权

**移动端认证流程**：
1. 客户端存储access_token(内存)和refresh_token(安全存储)
2. 每次API请求携带Authorization: Bearer {access_token}
3. 后端验证token有效性
4. token过期前10分钟自动用refresh_token刷新
5. refresh_token过期引导重新登录

**后台管理认证**：
- 账号密码登录+JWT Token
- 角色权限控制(RBAC)
- 操作日志记录

### 7.2 安全措施

| 安全项 | 移动端 | 后台管理 |
|--------|--------|----------|
| 传输加密 | HTTPS+TLS 1.3 | HTTPS+TLS 1.3 |
| 数据加密 | AES-256敏感字段 | AES-256密码 |
| API鉴权 | JWT | JWT |
| 防刷 | 限流+设备指纹 | 验证码+限流 |
| 内容安全 | AI审核+人工 | AI审核+人工 |
| 隐私合规 | 最小化收集 | 数据脱敏 |

---

## 8. 部署架构

### 8.1 部署拓扑

```
┌─────────────────────────────────────────────────────────────────────┐
│                        部署架构图                                    │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│   ┌─────────────┐                                                   │
│   │   CDN       │  静态资源加速                                      │
│   └──────┬──────┘                                                   │
│          │                                                          │
│   ┌──────▼──────┐                                                   │
│   │   负载均衡   │  Nginx/HAProxy                                   │
│   └──────┬──────┘                                                   │
│          │                                                          │
│   ┌──────▼──────────────────────────────────────────┐              │
│   │              应用服务 (Docker/容器)               │              │
│   │  ┌────────┐  ┌────────┐  ┌────────┐             │              │
│   │  │ 后端API │  │后台管理│  │  定时任务│             │              │
│   │  │ ASP.NET│  │ Vue3   │  │         │             │              │
│   │  └────────┘  └────────┘  └────────┘             │              │
│   └──────┬──────────────────────────────────────────┘              │
│          │                                                          │
│   ┌──────▼──────┐    ┌──────┐    ┌──────────────┐                 │
│   │  PostgreSQL │    │Redis │    │  本地存储     │                 │
│   │  (生产)     │    │      │    │  (图片/文件)  │                 │
│   └─────────────┘    └──────┘    └──────────────┘                 │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 8.2 开发/生产环境

| 环境 | 数据库 | 文件存储 | 部署方式 |
|------|--------|----------|----------|
| 开发 | SQLite | 本地文件夹 | IIS/Docker |
| 生产 | PostgreSQL | 本地存储/DNAS | Docker |

### 8.3 容灾设计

| 容灾项 | 方案 |
|--------|------|
| 数据库 | 定期备份 |
| 缓存 | Redis持久化 |
| 存储 | 定期备份 |
| 服务 | Docker健康检查+自动重启 |

---

## 9. 模块依赖关系

### 9.1 模块间依赖

```
模块依赖关系
─────────────
           M1  M2  M3  M4  M5  会员  推送  支付  后台
┌─────────┼────┼────┼────┼────┼─────┼────┬────┬─────┐
M1注册登录│ -  │ ○  │ ○  │ ○  │ ○   │    │    │     │
M2照片生成│ ○  │ -  │ ○  │    │ ●   │ ○  │    │     │
M3论坛    │ ○  │ ○  │ -  │ ○  │ ○   │    │    │     │
M4模板    │ ○  │ ●  │ ○  │ -  │ ○   │    │    │     │
M5色号推荐│ ○  │ ●  │    │    │ -   │    │    │     │
会员系统  │    │ ○  │    │    │     │ -  │    │     │
推送服务  │ ○  │ ○  │ ○  │ ○  │ ○   │    │ -  │     │
支付系统  │    │ ○  │    │    │     │ ○  │    │ -   │
后台管理  │    │ ○  │ ○  │ ○  │ ○   │ ○  │ ○  │ -   │
└─────────┴────┴────┴────┴────┴─────┴────┴────┴─────┘
● = 强依赖  ○ = 弱依赖
```

### 9.2 核心业务流程

**主业务流程**：
```
注册/登录 (M1) → 选图 (M2) → 编辑 (M2) → 参数配置 (M2)
    → AI生成 (调用云服务) → 预览 (M2) → 色号表 (M5) → 导出/分享
```

**社区流程**：
```
发布帖子 (M3) → 审核 → 展示 → 互动 → 消息通知
```

**模板流程**：
```
模板库 (M4) → 按此生成 → M2创作页 → 结果
```

---

## 10. 性能设计

### 10.1 性能指标

| 指标 | 移动端 | 后台管理 | 后端API |
|------|--------|----------|---------|
| 启动/加载 | <1.5s | <1s | - |
| 响应时间 | - | - | P95<300ms |
| AI生成 | 依赖云服务 | - | - |
| 信息流首屏 | <1s | - | - |
| 列表滑动 | 60fps | - | - |

### 10.2 性能优化策略

- **缓存**：本地缓存+Redis，热点数据预加载
- **CDN**：静态资源加速，图片懒加载
- **数据库**：索引优化，SqlSugar查询优化
- **异步**：后台任务处理

---

## 11. 监控体系

### 11.1 监控维度

| 维度 | 内容 |
|------|------|
| 基础设施 | CPU/内存/磁盘/网络 |
| 应用服务 | API响应时间/错误率 |
| 业务指标 | 注册数/生成次数/DAU |

### 11.2 告警策略

| 级别 | 场景 | 响应时间 |
|------|------|----------|
| P0 | 服务不可用 | 5分钟 |
| P1 | 核心接口延迟>1s | 15分钟 |
| P2 | 错误率>5% | 30分钟 |

---

## 12. 附录

### 12.1 技术选型说明

| 选型 | 原因 |
|------|------|
| C# ASP.NET | 高性能、稳定、微软生态完善 |
| SqlSugar | 轻量级ORM，支持多数据库 |
| SQLite | 开发便捷，无需安装 |
| PostgreSQL | 生产环境高并发支持 |
| 本地存储 | 简化部署，降低成本 |
| 不使用网关 | 当前阶段不需要，统一认证在服务层实现 |
| 不使用消息队列 | 当前阶段无高并发场景，使用后台任务处理 |

### 12.2 版本历史

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| v0.1 | 2026-06-07 | - | 初稿，基于PRD M1-5 |
| v0.2 | 2026-06-07 | - | 修改技术栈：C# ASP.NET、SqlSugar、SQLite/PostgreSQL、本地存储、调用AI云服务、去除网关和消息队列 |

### 12.3 关联文档

- 《PRD_M1_注册登录_v0.1.md》
- 《PRD_M2_照片转拼豆_v0.1.md》
- 《PRD_M3_论坛_v0.1.md》
- 《PRD_M4_经典拼图模板_v0.1.md》
- 《PRD_M5_色号推荐_v0.1.md》
- 《移动端需求规格说明书》
- 《后台管理系统需求规格说明书》

---

*总体架构设计文档结束*